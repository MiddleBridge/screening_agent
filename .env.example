ANTHROPIC_API_KEY=sk-ant-...
GMAIL_CREDENTIALS_PATH=credentials.json
GMAIL_TOKEN_PATH=token.json
GMAIL_PROCESSED_LABEL=Inovo/Screened
GMAIL_USER_EMAIL=your@gmail.com
REVIEWER_EMAIL=your@gmail.com
POLLING_INTERVAL_MINUTES=15
GATE2_PASS_THRESHOLD=6.0

# Optional: Notion pipeline sync (python main.py --sync-notion --days 30)
# NOTION_API_KEY=secret_xxx
# NOTION_DATABASE_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Optional: Notion tasks database used by one-shot call sync
# NOTION_TASKS_DATABASE_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# No Tavily/SerpAPI required. Default: no paid web search (OpenAI-only).
# Optional paid snippets: set TAVILY_API_KEY. To ignore any key: EXTERNAL_WEB_SEARCH=0
# EXTERNAL_WEB_SEARCH=0

# Optional: deterministic HQ resolver (no LLM).
# Uses web-search snippet parsing (typically LinkedIn "Headquarters: ...").
# OFF by default to avoid unexpected paid search usage.
# HQ_RESOLVER=1
# HQ_RESOLVER_MAX_RESULTS=5

# Optional: deterministic Stage resolver (no LLM).
# Uses web-search snippet parsing ("Stage: Seed", "Seed round", etc.).
# OFF by default to avoid unexpected paid search usage.
# STAGE_RESOLVER=1
# STAGE_RESOLVER_MAX_RESULTS=6

# Website mandate: strict geo = block scoring on any geography_match=false (default: only if FAIL_CONFIDENT or HIGH confidence).
# WEBSITE_MANDATE_STRICT_GEOGRAPHY=1

# --- Fireflies (optional): Webhooks V2 + GraphQL → founder_calls_json + Notion memo
# API key: https://app.fireflies.ai/integrations/custom/fireflies
# Webhook V2: https://app.fireflies.ai/integrations/api/webhook — use HTTPS URL; same signing secret as below.
# Local: python main.py --fireflies-hook → POST http://127.0.0.1:9848/fireflies/webhook (use ngrok/etc. for Fireflies).
# FIREFLIES_API_KEY=
# FIREFLIES_WEBHOOK_SIGNING_SECRET=
# FIREFLIES_HOOK_PORT=9848
# FIREFLIES_HOOK_BIND=127.0.0.1
# FIREFLIES_TITLE_MATCH_DAYS=120
# FIREFLIES_MAX_TRANSCRIPT_CHARS=48000
# FIREFLIES_FETCH_TRANSCRIPT=1
